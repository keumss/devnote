---
title: "Part 3. 고급: 폼 처리와 컴포넌트 재사용"
---

## HTML Form 및 FastAPI 라우팅 결합

새로운 블로그 포스트를 작성하는 폼입니다. 폼의 action과 FastAPI의 엔드포인트를 연결하고, 에러 메시지가 있을 경우 출력하도록 구성합니다.

```html
<!-- app/templates/create_post.html -->
{% extends "base.html" %}

{% block content %}
    <h2>새 포스트 작성</h2>

    {% if error_message %}
        <div class="alert alert-danger">{{ error_message }}</div>
    {% endif %}

    <!-- 파일(썸네일 등)을 함께 업로드하려면 enctype="multipart/form-data" 필수 -->
    <form action="/posts/" method="post" enctype="multipart/form-data">
        <div class="form-group">
            <label for="title">제목</label>
            <input type="text" id="title" name="title" required value="{{ form_data.title | default('') }}">
        </div>

        <div class="form-group">
            <label for="content">본문</label>
            <textarea id="content" name="content" rows="10" required>{{ form_data.content | default('') }}</textarea>
        </div>

        <div class="form-group">
            <label for="thumbnail">썸네일 이미지</label>
            <input type="file" id="thumbnail" name="thumbnail" accept="image/*">
        </div>

        <button type="submit" class="btn btn-primary">저장하기</button>
    </form>
{% endblock %}
```

## 매크로 (Macros) - 재사용 가능한 컴포넌트

블로그의 포스트 카드(Post Card)나 페이지네이션 버튼처럼 여러 템플릿에서 반복적으로 사용되는 UI 요소를 매크로로 정의하여 재사용합니다.

```html
<!-- app/templates/macros/post_card.html -->
{% macro render_post_card(post) %}
    <div class="post-card">
        {% if post.thumbnail %}
            <img src="{{ post.thumbnail }}" alt="{{ post.title }} 썸네일">
        {% else %}
            <img src="/static/images/default-thumb.png" alt="기본 썸네일">
        {% endif %}

        <div class="post-card-content">
            <h3><a href="/posts/{{ post.id }}">{{ post.title }}</a></h3>
            <p class="summary">{{ post.content[:80] }}...</p>
            <small>{{ post.author.username }} | {{ post.created_at.strftime("%Y-%m-%d") }}</small>
        </div>
    </div>
{% endmacro %}

<!-- 다른 템플릿에서 불러와서 사용하기 -->
<!--
{% from "macros/post_card.html" import render_post_card %}

<div class="grid">
    {% for post in posts %}
        {{ render_post_card(post) }}
    {% endfor %}
</div>
-->
```

## URL을 하드코딩하지 않기

경로 문자열을 템플릿에 직접 반복하면 라우트를 바꿀 때 링크가 쉽게 깨집니다. FastAPI 라우트에 이름을 지정하고 `url_for()`로 URL을 만들면 경로 변경의 영향을 한 곳으로 줄일 수 있습니다.

```python
from fastapi import Request
from fastapi.responses import RedirectResponse

@app.post('/posts', name='create_post')
def create_post(request: Request):
    # 저장 로직 뒤에는 이름으로 URL을 만들어 리다이렉트합니다.
    return RedirectResponse(request.url_for('render_home'), status_code=303)
```

```html
<!-- 경로 문자열 대신 라우트 이름을 사용합니다. -->
<form action="{{ url_for('create_post') }}" method="post">
    <input name="title" required>
    <button type="submit">저장</button>
</form>
```

## 누락된 값과 접근성 있는 폼 오류 표시

서버 검증에 실패했을 때 사용자가 어떤 필드를 고쳐야 하는지 연결해 알려줍니다. `id`, `for`, `aria-describedby`를 맞추고 입력한 값은 다시 보여 주되, 비밀번호 같은 민감한 값은 되돌려 렌더링하지 않습니다.

```html
{% set title_error = errors.get('title') if errors else none %}
<label for="title">제목</label>
<input
    id="title"
    name="title"
    value="{{ form_data.title | default('') }}"
    aria-describedby="title-error"
    aria-invalid="{{ 'true' if title_error else 'false' }}"
>
{% if title_error %}
    <p id="title-error" role="alert">{{ title_error }}</p>
{% endif %}
```
